# Pinia

## 2.0 → 3.0 (2025-02-11)

**[Changes](https://github.com/vuejs/pinia/releases/tag/v3.0.0)**

- Now use the native `Awaited` in TS 4.5.
- Replace `PiniaStorePlugin` with `PiniaPlugin`.
- Replace `defineStore({ id: 'id' })` with `defineStore('id')`.
- Now Pinia is published as ESM.
